var config = {

    //调试模式 (开启后左侧菜单不缓存,菜单收展失效,打开渲染速度会变慢)
    debug: true,

    //网站名称 (左上角显示的文字LOGO)
    webname: 'zkadmin',

    //菜单列表路径 (可以是本地json,也可以是api接口)
    menuUrl: '/manage/menu',

    //layer全局提示层
    layerMsg: {
        offset: 't', //坐标
        shade: [0.4, '#000'] //遮罩 
    }
}

